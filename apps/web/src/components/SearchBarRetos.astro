  <div class="w-full bg-white p-4 rounded-2xl shadow-sm border border-slate-100 flex flex-col md:flex-row gap-4 items-center">
  
  <div class="relative w-full md:flex-1">
    <span class="absolute inset-y-0 left-0 flex items-center pl-4 pointer-events-none text-slate-400">
      <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
      </svg>
    </span>
    <input 
      type="text" 
      placeholder="Buscar retos por título o descripción..." 
      class="w-full pl-11 pr-4 py-3 bg-slate-50 border border-transparent rounded-xl text-slate-700 placeholder-slate-400 focus:outline-none focus:bg-white focus:border-brand-500 transition-colors"
    />
  </div>

  <div class="w-full md:w-48 relative">
    <select class="w-full appearance-none px-4 py-3 bg-slate-50 border border-transparent rounded-xl text-slate-800 font-medium focus:outline-none focus:bg-white focus:border-brand-500 transition-colors cursor-pointer">
      <option value="todos">Todos</option>
      <option value="green-tech">Green Tech</option>
      <option value="social">Impacto Social</option>
    </select>
    <span class="absolute inset-y-0 right-0 flex items-center pr-4 pointer-events-none text-slate-400">
      <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
      </svg>
    </span>
  </div>

  <div class="w-full md:w-48 relative">
    <select class="w-full appearance-none px-4 py-3 bg-slate-50 border border-transparent rounded-xl text-slate-800 font-medium focus:outline-none focus:bg-white focus:border-brand-500 transition-colors cursor-pointer">
      <option value="todos">Todos</option>
      <option value="abierto">Abierto</option>
      <option value="evaluacion">En evaluación</option>
      <option value="desarrollo">En desarrollo</option>
    </select>
    <span class="absolute inset-y-0 right-0 flex items-center pr-4 pointer-events-none text-slate-400">
      <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
      </svg>
    </span>
  </div>

</div>