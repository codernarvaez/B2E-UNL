{
  "name": "b2e-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix apps/web -- --host 127.0.0.1 --port 4321",
    "dev:api": "cd apps/api && uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "install:all": "npm install --prefix apps/web && pip install -e apps/api[dev]"
  }
}
